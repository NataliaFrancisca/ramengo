export interface ICard {
    id: string,
    name: string,
    description: string,
    imageActive: string,
    imageInactive: string,
    price: number
}

export interface ISuccessResponse{
    id: string,
    description: string,
    image: string,
}

